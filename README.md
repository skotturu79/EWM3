# EWM3
EWM Door Agent app

## Door agent

`door_agent.py` provides a small `DoorAgent` that tracks a door's open/locked state and supports:

- `request_open()`
- `request_close()`
- `request_lock()`
- `request_unlock()`

Run tests with:

```bash
python -m unittest -v
```
